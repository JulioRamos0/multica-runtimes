# Docker Image Registry (e.g., ghcr.io/username)
REGISTRY=ghcr.io/julioramos0

# Git Identity (Used by agents to commit and push code)
GIT_USER_NAME="Your Name"
GIT_USER_EMAIL="your.email@example.com"
